import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import UyumLanding from './landing-pages/uyum-landing'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <UyumLanding />
  </React.StrictMode>,
)
